---
version: 0.1
frontend:
  phases:
    preBuild:
      commands:
      - npm install -g
    build:
      commands:
      - REACT_APP_ENV_MYENV=test npm run build
  artifacts:
    baseDirectory: "./build"
    files:
    - "**/*"
  cache:
    paths: node_modules/**/*
