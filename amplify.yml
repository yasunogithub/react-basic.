version: 1
applications:
  - frontend:
      phases:
        preBuild:
            commands:
                - yarn install
        build:
          commands: ["yarn build"]
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: build
