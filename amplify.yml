version: 1
applications:
  - frontend:
      phases:
        preBuild:
            commands:
                - yarn install
        build:
          commands: ["yarn build"]
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: public
