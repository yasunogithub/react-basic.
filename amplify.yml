---
version: 0.1
frontend:
  phases:
    preBuild:
      commands:
      - npm install -g
    build:
      commands:
      - REACT_APP_LIFF_ID=1654037541-x5EqAEEA REACT_APP_USER_ID=1234 npm run build
  artifacts:
    baseDirectory: "./build"
    files:
    - "**/*"
  cache:
    paths: node_modules/**/*
