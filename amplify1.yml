---
version: 0.1
frontend:
  phases:
    preBuild:
      commands:
      - npm install -g
    build:
      commands: hogehoge
  artifacts:
    baseDirectory: "./build"
    files:
    - "**/*"
  cache:
    paths: node_modules/**/*
